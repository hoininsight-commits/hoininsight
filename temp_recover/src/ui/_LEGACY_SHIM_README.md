# ⚠️ LEGACY SHIM LAYER

이 디렉토리(`src/ui/`)는 이전 버전과의 호환성을 위한 **Alias(Shim)** 전용 공간입니다.

### 규칙
1. **신규 로직 추가 금지**: 모든 새로운 UI 로직은 `src/ui_logic/`에 작성하십시오.
2. **계산/판단 금지**: 이 폴더의 파일은 오직 `src/ui_logic/`의 함수를 호출하여 반환하는 역할만 수행해야 합니다.
3. **이전 권장**: 가능하면 기존 코드의 import 경로를 `src.ui_logic`으로 직접 변경하십시오.

---
마지막 구조 리팩토링: REF-006 (2026-02-06)
