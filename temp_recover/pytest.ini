[pytest]
addopts = --continue-on-collection-errors -v --tb=short
markers =
    core: Core constitutional tests
    optional: Optional tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
