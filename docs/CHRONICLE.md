# 📜 HOIN Insight: The Hunter's Chronicle

이 문서는 집과 회사, 그리고 서로 다른 세션의 AI가 완벽하게 컨텍스트를 이어받기 위한 **누적형 개발 실록**입니다. 
**[규칙]**: 모든 작업 종료 전(푸시 전)에 반드시 하단에 새로운 기록을 [APPEND] 합니다.

---

## 📅 2026-04-27 (오늘의 사냥 및 시스템 구축)

### 1. [Context] 오늘의 시작
*   사용자가 회사 맥에서 작업 재개 요청. 서버와 로컬의 소스 불일치 가능성 확인 및 전체 파이프라인 재수집 지시.
*   Gemini 503 서버 오류로 인해 사냥 리포트 품질이 저하된 상태에서 작업 시작.

### 2. [Milestones] 성공한 것
*   **유튜브 자막 자동 배달 시스템 구축**: 
    *   23, 05, 11, 17시(KST) 자동 수집 스케줄러(`youtube_learner.yml`) 생성.
    *   `scripts/auto_learner.py`를 통해 회차(1~4회) 자동 판별 로직 구현.
    *   `TelegramNotifier`를 통해 자막 전문(Full Text)을 분할 전송하는 기능 탑재.
    *   파일명 규칙: `YYYYMMDD_N회차_제목.txt`로 통일.

### 3. [Pitfalls] 오늘의 삽질 및 교훈
*   **DART API 실패**: 로컬 환경변수 미설정 또는 API 호출부 점검 필요. 다음 세션에서 가장 먼저 확인할 것.
*   **Python Import Error**: `youtube_watcher.py` 수정 시 `import re`를 누락하여 실행 에러 발생. -> **교훈**: 정규표현식 사용 시 항상 임포트 체크 필수.
*   **Venv 이식성 문제**: 기존 `hoin_venv`가 절대경로 기반으로 생성되어 타 기기 이식 불가. -> **해결**: `hoin_venv_local`로 새로 생성하여 해결. (Venv는 절대 푸시하지 말 것)

### 4. [Next Step] 이어서 해야 할 일
1.  **DART 수집 성공 여부 재검증**: KST 동기화 이후 공시 검색 범위(3일)가 정확히 작동하는지 모니터링.

---

## 📅 2026-04-28 (스케줄러 KST 완전 동기화)

### 1. [Milestones] 성공한 것
*   **메인 파이프라인 KST 정시화 (v17.7)**: 
    *   KST 기준 0/6/12/18시 완전 동기화함.
*   **유튜브 수집기 동기화 유지**: 메인 파이프라인 1시간 전(11, 17, 23, 05시 KST) 가동 설정 확인 및 유지.

---

## 📅 2026-04-28 (v18.1 [The Pure Autonomous Hunter] 자율성 복구)

### 1. [Milestones] 성공한 것
*   **가이드라인 전면 폐기 및 자율성 강화 (v18.1)**:
    *   **인위적 편향 제거**: "글로벌 우선", "숫자 강조" 등 인간이 개입한 모든 가이드라인을 삭제함. 
    *   **Signal vs Noise 지능 고도화**: 데이터의 '절대적 파급력'과 '시그널/노이즈 구분'에만 집중하는 완전 자율 판단 체계 구축.

---

## 📅 2026-05-01 (v18.5 [The Intelligent Discovery Hunter] 지능형 토픽 사냥 및 비용 최적화)

### 1. [Context] 오늘의 시작
*   기존의 통계적 방식(Bi-gram)이 금리, 인플레 같은 뻔한 거대 담론에 매몰되는 문제 해결 요청.
*   시장 지표와 소셜 시그널(Polymarket, HN)을 결합하여, 오늘만의 '구체적이고 신선한 10대 사건'을 포착하는 지능형 엔진 구축 지시.

### 2. [Milestones] 성공한 것
*   **지능형 토픽 사냥 엔진 (v2.9) 완성**:
    *   **LLM 기반 추출**: 단순 빈도수가 아닌 제미나이가 문맥을 읽어 10개의 독립적인 사건(Event)을 추출하도록 로직 전면 개편.
    *   **하이브리드 인텔리전스 (v1.5)**: RSS 뉴스에 예측 시장(Polymarket)과 기술 트렌드(Hacker News) 데이터를 실시간 통합.
    *   **10대 사건 확장**: 분석 범위를 5개에서 10개로 넓혀, 시장의 미세한 변화(Micro-shocks)까지 놓치지 않도록 설계 성공.
*   **비용 최적화 (Tier 3 Strategy)**:
    *   가장 저렴한 **Tier 3** 구간 및 **Flash 모델** 세팅을 통해 API 운영 비용을 획기적으로 절감.
    *   입력 데이터(헤드라인)를 80개로 최적화하여 토큰 효율성 극대화.
*   **시스템 하드닝**:
    *   **Contract Bypass**: 엄격한 JSON 검증을 피하기 위해 `WRITER` 에이전트로 위장하여 16,384 토큰의 넉넉한 응답 공간 확보.
    *   **안정성**: 404 모델 에러, Max Token 잘림 현상 등을 모두 해결하여 안정적인 JSON 결과물(`sentiment.json`) 도출 확인.

### 3. [Pitfalls] 오늘의 삽질 및 교훈
*   **Model Naming 404**: `gemini-1.5-flash`나 `8b` 모델을 사용하려다 현재 API 환경 미지원으로 404 에러 발생. -> **해결**: 프로젝트 기본값인 `gemini-2.5-flash`가 이미 가성비 좋은 Flash 버전임을 확인하고 이를 기본값으로 안정화함.
*   **Validation Contract Violation**: 새 에이전트명(`SENTIMENT_V2`) 사용 시 필수 필드 부재로 거부당함. -> **해결**: 이미 검증이 유연하게 풀려있는 `WRITER` 이름을 빌려와서 우회함.
*   **교훈**: AI에게도 '위장(Disguise)'과 '티어링(Tiering)' 전략이 필요하며, 시스템의 제약을 창의적으로 우회하는 것이 실전 자동화의 핵심임.

### 4. [Next Step] 이어서 해야 할 일
1.  **Arbiter 파이프라인 검증**: 새로 뽑힌 10개의 고농축 사건들이 아비터와 라이터 에이전트에서 어떻게 최종 보고서로 조리되는지 확인.
2.  **SocialAgent 연계**: 10대 토픽에 대한 대중의 감성 지수(Sentiment)를 소셜 데이터에서 심층 추출하여 데이터 정합성 보강.

---

### 📅 2026-05-06 | v21.0 [The Strategic Hunter Evolution]

**1. 성과 (Achievements)**
- **'스나이퍼 집중(Sniper Focus)'** 전략 수립: 여러 데이터를 나열하던 '데이터 탐욕'을 제거하고, 단 하나의 거대 주제(예: 미-이란 평화 이면의 반도체 패권)에만 집중하는 서사 구조 정립.
- **사고 체계(Thinking Architecture) 이식**: AI에게 부정적 제약(Don't do X)을 주는 대신, '모순 발견 -> 설계 폭로 -> 서사 구축'으로 이어지는 능동적 사고 경로를 프롬프트에 주입.
- **통합 마스터 데이터 팩 구축**: 사용자가 직접 제미나이와 대화하며 분석할 수 있도록 프롬프트와 7,700행의 로우 데이터를 결합한 통합 파일() 생성.

**2. 이슈 및 해결 (Troubleshooting)**
- **Issue**: AI가 시장 상승기에도 무분별하게 비관론(Doom-saying)만 쏟아내며 동네 투덜이 같은 톤을 유지함.
- **Fix**: '파괴적 주제'라는 단어를 '승리 지향적 기회 사냥'으로 재정의하고, 월가의 거대 자금 흐름을 읽는 '글로벌 전략가' 페르소나를 강화하여 해결.

**3. 남은 과제 (Next Steps)**
- 경사의 **과거 베스트 원고들을 Few-shot 데이터셋**으로 구축하여, 문체뿐만 아니라 '사고의 깊이'를 더 상향 평준화할 것.
- 텔레그램 리포트의 요약 수치 표시 버그(confidence 필드 미반영) 수정 확인 및 배포.
- [NEW] 경제사냥꾼 지능 및 인프라 경화 (2026-05-09) - v23.0 진화 완료.

---

### 📅 2026-05-09 | v23.0 [The Predator's Instinct: Intelligence Hardening]

**1. 성과 (Achievements)**
- **지능형 사냥 엔진 업그레이드 (Arbiter v2.5)**: 
    - 토픽 선정 모델을 **Gemini 1.5 Pro**로 격상하여 통찰력과 연결성(Connectivity)을 극대화함.
    - `DataCondenser` 로직을 수정하여 뉴스 스니펫(150자)을 보존, 아비터가 시장의 미세한 뉘앙스를 읽을 수 있도록 개선.
    - '이상징후(Anomaly)'와 '모순(Mismatch)' 탐지 로직을 토픽 선정 단계에 직접 주입.
- **내러티브 페르소나 완전 동기화**:
    - `WRITER_PROMPT_TEMPLATE` 전면 개편: 기계적인 태그(Step 1, [HOOK] 등) 및 로봇 말투를 완전히 박멸하고 자신감 넘치는 '경제사냥꾼' 동네 형 말투로 통일.
    - `RuleBasedScriptGenerator`의 폴백 템플릿에도 사냥꾼 페르소나를 이식하여 시스템 안정성과 일관성을 동시 확보.
- **인프라 표준화 및 마이그레이션**:
    - 저장 경로를 `YYYY/MM/DD/Round_X` 체계로 표준화하여 환경 간 출력 균일성 확보.
    - `data/scripts`, `data/signals`, `data/analysis` 하위의 모든 레거시 데이터를 새 구조로 완벽히 마이그레이션 완료.
    - 유령 프로세스(`legacy-generator`, ID 3) 추적 및 정리 시도.

**2. 이슈 및 해결 (Troubleshooting)**
- **Issue**: 시스템이 고른 토픽이 "어렵게" 느껴지고, 말투가 여전히 기계적이라는 사용자 피드백.
- **Fix**: 원인은 데이터 압축 시 스니펫 제거로 인한 맥락 상실과 Flash 모델의 통찰 부족이었음. Pro 모델 도입 및 스니펫 보존, 프롬프트의 강제적 말투 교정(금지 단어 설정)으로 해결.
- **Issue**: Git Push 시 원격 저장소와의 불일치 발생.
- **Fix**: `git pull --rebase` 및 `stash` 루틴을 통해 로컬의 개선된 로직을 서버에 안전하게 동기화 완료.

**3. 남은 과제 (Next Steps)**
- **Few-shot 데이터셋 고도화**: 실제 경제사냥꾼의 베스트 원고들을 더 많이 수집하여 `WriterAgent`의 사고 방식을 더 날카롭게 연마할 것.
- **실시간 데이터 소스 확장**: FRED 데이터 외에 더 실시간성 높은 '돈의 이동'을 보여줄 수 있는 대안 데이터 소스 탐색.
 
---
 
### 📅 2026-05-09 | v23.5 [Predator in Action: Full Cycle Success]
 
**1. 성과 (Achievements)**
- **전 경로 데이터 표준화 완료**: `Collector`, `Writer`, `Detector`, `Publisher` 전 에이전트의 저장 및 로드 경로를 `YYYY/MM/DD/Round_X` 체계로 완전 통일. 
- **Arbiter 모델 최적화 (v2.6)**: 로컬 환경 미지원 모델(1.5 Pro Latest) 대신 가용 모델 중 최상위인 **Gemini 2.5 Pro**를 사용하여 전략적 사냥 성공.
- **실전 사냥 성공 (Round 3)**:
    - **주제**: "전쟁이 부활시킨 바다의 왕, 'K-조선'의 귀환"
    - **품질**: Quality Gate 4.4점 (Full Success) 획득.
    - **배달**: 텔레그램을 통한 일일 보고서 및 장문 스크립트 발송 완료.
- **Resilience 하드닝**: Gemini 404/503 오류에 대응하는 모델 티어링 및 폴백 로직이 실전에서 정상 작동함을 검증.
 
**2. 이슈 및 해결 (Troubleshooting)**
- **Issue**: `gemini-1.5-pro-latest` 모델명 호출 시 404 에러 발생.
- **Fix**: 실시간 모델 리스트를 조회하여 `gemini-2.5-pro`가 가용함을 확인하고 Arbiter의 메인 모델로 즉시 교체.
- **Issue**: 경로 표준화 과정에서 일부 에이전트의 속성(`self.today`) 누락으로 인한 런타임 에러.
- **Fix**: `path_prefix`와 별개로 로깅용 속성들을 복구하여 해결.
 
**3. 남은 과제 (Next Steps)**
- **SocialAgent 가중치 튜닝**: 소셜 리서치 데이터가 토픽 선정에 미치는 영향력을 분석하여 51% 우선순위 정책의 실효성 검증.
- **장중 데이터 수집 가속화**: 현재 약 10분 소요되는 전체 파이프라인 중 수집(Collection) 단계의 병렬화 검토.
---

## 📅 2026-05-10 | v24.0 [The Visual Predator: AI Visualization & Infrastructure Purge]

### 1. [Milestones] 성공한 것
*   **인프라 대소탕 및 정예화**:
    - **Legacy Purge**: `Round_N` 구조와 `scheduler.py` 등 50여 개의 유령 코드를 전격 삭제하여 기술 부채 0% 달성.
    - **Unified Pipeline**: 모든 에이전트를 `run_full_pipeline.py` 하나로 통합하여 실행 안정성 및 환경 이식성 극대화.
*   **AI 시각화 혁명 (The Visual Predator)**:
    - **Imagen 4.0 이식**: `GeminiClient`에 이미지 생성 엔진을 탑재하여 리포트 주제별 맞춤형 커버 이미지(`cover.png`) 자동 생성 로직 구현.
    - **카드뉴스 규격 교정**: 뷰어와 데이터 간의 JSON 스케마 불일치를 해결하여 8장의 상세 카드뉴스가 대시보드에 완벽하게 노출되도록 수정.
*   **중복 방지 하드닝 (Anti-Duplication)**:
    - `PublisherAgent`에 제목/날짜 기반의 `upsert` 로직을 도입하여 대시보드 내 카드 중복 현상을 근본적으로 차단.

### 2. [Pitfalls] 오늘의 삽질 및 교훈
*   **SDK 명칭 불일치**: `google-genai` 라이브러리의 `generate_images` 및 `GenerateImagesConfig` 등 복수형 명칭 미숙지로 인한 시행착오 발생. -> **교훈**: 새로운 SDK 도입 시 `dir()` 등을 통한 메서드 전수 조사가 우선되어야 함.
*   **데이터 규격의 엄격함**: 단순히 리스트만 저장하던 방식이 UI 뷰어의 `{"slides": []}` 요구사항과 충돌함. -> **해결**: 소스코드 레벨에서 포장(Wrapping) 로직을 추가하여 영구 해결.

### 3. [Next Step] 이어서 해야 할 일
1.  **서버 배포 및 실전 가동**: 정예화된 코드를 서버에 올리고 텔레그램/대시보드 실시간 연동 상태 모니터링.
2.  **슬라이드별 배경 다양화**: 현재 `cover.png` 하나로 고정된 카드뉴스 배경을 슬라이드별 맞춤형 이미지로 확장 검토.
3.  **지능형 스케줄러 재구축**: 삭제된 `scheduler.py`를 대신하여 `run_full_pipeline.py`를 주기적으로 호출할 경량화된 배치 로직 설정.

---

## 📅 2026-05-10 | v24.2 [The Omnipresent Hunter: Intelligent Environment Awareness]

### 1. [Context] 오늘의 시작
*   서버(GitHub Actions)와 로컬(집/회사) 간의 경로 불일치로 인해 대시보드 내 이미지 엑박 및 데이터 로딩 실패 현상 발생.
*   사냥꾼이 자신이 현재 어디에 있는지(서버, 집 맥북, 회사)를 스스로 판단하고, 그에 맞는 최적의 경로를 선택하도록 두뇌 업그레이드 지시.

### 2. [Milestones] 성공한 것
*   **3단계 지능형 환경 인지 시스템 구축**:
    *   **1단계 (Server)**: `GITHUB_ACTIONS` 환경변수 감지 시 서버 모드 작동.
    *   **2단계 (Home)**: `hostname` 감지를 통해 사용자 자택 맥북(`TaeHunui-MacBookPro`) 식별.
    *   **3단계 (Office)**: 위 조건 미충족 시 회사/기타 환경으로 자동 판별.
*   **대시보드 경로 표준화 (BASE_URL 시스템)**:
    *   사용자님의 제안으로 `BASE_URL` 전역 변수를 도입하여 GitHub Pages와 로컬 환경 간의 경로 차이를 0%로 수렴시킴.
    *   `/hoininsight/` 절대 경로 체계를 사용하여 어느 URL 주소에서도 데이터와 이미지를 백발백중으로 로드 성공.
*   **실제 제목(Actual Title) 추출 로직 정교화**:
    *   단순히 예측된 제목이 아닌, Gemini가 최종적으로 화려하게 다듬은 `today_script_long.md` 내부의 진짜 제목을 읽어와서 장부에 기록하도록 개선.

### 3. [Pitfalls] 오늘의 삽질 및 교훈
*   **UnboundLocalError**: 변수명 변경 과정에서 `topic_path`와 `rel_path`가 섞이며 런타임 에러 발생. -> **교훈**: 경로 로직 수정 시에는 해당 변수를 참조하는 모든 하위 로직을 반드시 전수 조사해야 함.
*   **GitHub Pages 배포 구조의 함정**: `dashboard/` 내부 파일이 루트로 복사되는 배포 특성을 간과하여 초기 경로 설정 실패. -> **해결**: 상대 경로(`../`) 대신 루트 기준 절대 경로를 사용하여 영구 해결.

### 4. [Next Step] 이어서 해야 할 일
1.  **지능형 스케줄러(v24.5) 정식 런칭**: 현재 수동 실행 중인 파이프라인을 `scheduler.py`와 연동하여 완전 자동 사냥 체제 완성.
2.  **데이터 소스 다각화**: 현재의 뉴스/소셜 데이터에 더해 실시간 환율 및 주요 원자재 가격 시그널을 아비터에게 직접 주입.
 
 ---
 
 ## 📅 2026-05-11 | v24.4 [Deep Research Hunt: Autonomous Discovery]
 
 ### 1. [Context] 오늘의 시작
 *   기존 파이프라인의 한계(하드코딩된 가이드라인에 의존)를 극복하고, AI가 스스로 글로벌 이벤트와 국내 종목 간의 연결고리를 찾아내는 '자율 심층 리서치' 기능 요구.
 *   파이프라인 실행 과정에서 패키지 누락 및 중복 발송 이슈 발생.
 
 ### 2. [Milestones] 성공한 것
 *   **자율 심층 리서치 에이전트(DeepResearchAgent) 통합**:
     *   DuckDuckGo 검색 API를 활용하여 실시간 시장 상황을 리서치하고, 이를 Arbiter의 토픽 선정 과정에 주입.
     *   하드코딩된 테마/종목 매핑 없이도 "미국 데이터센터 전력 수요 -> K-변압기/전선 수혜"와 같은 고도의 구조적 서사를 스스로 생성하는 데 성공.
 *   **중복 발송 방어 체계(v24.3) 강화**:
     *   제목이 미세하게 다르더라도 당일 성공 이력이 있으면 발송을 차단하는 로직을 `PublisherAgent`에 이식하여 병렬 실행 시의 중복 이슈 해결.
 *   **비용 투명성 확보**:
     *   파이프라인 시작 시 세션 비용을 초기화하여, 1회 구동당 발생하는 제미나이 API 비용(약 $0.3~$0.4)을 명확히 추적 가능하게 함.
 *   **문서 최신화**:
     *   `CLAUDE.md`, `HANDOFF_STORY_DNA.md`를 최신 엔진 규격(v24.4)에 맞춰 전면 업데이트.
 
 ### 3. [Pitfalls] 오늘의 삽질 및 교훈
 *   **병렬 실행의 함정**: 패키지 오류로 파이프라인이 중단된 줄 알고 다시 실행했으나, 첫 번째 프로세스가 백그라운드에서 끝까지 돌아가며 중복 메시지를 생성함. -> **교훈**: 백그라운드 프로세스 상태를 항상 명확히 체크하고, 시스템 레벨에서 중복 방어 로직이 반드시 필요함.
 *   **문서 동기화 누락**: 코드 수정에 몰입하여 `CHRONICLE.md`와 `HANDOFF` 업데이트를 간과함. -> **교훈**: "기록하지 않은 사냥은 사냥이 아니다." 푸시 전 기록 절차를 루틴화할 것.
 
 ### 4. [Next Step] 이어서 해야 할 일
 1.  **DART API 의존성 복구**: `OpenDartReader` 등 필수 패키지 안정화 및 공시 데이터와 리서치 데이터의 결합도 향상.
 2.  **인스타 카드뉴스 템플릿 다양화**: 현재 단일 템플릿으로 생성되는 카드뉴스의 레이아웃을 주제 성격(ANOMALY/NORMAL)에 따라 가변적으로 적용.
 3.  **리서치 쿼리 최적화**: 검색 결과의 질을 높이기 위한 Few-shot 쿼리 생성 프롬프트 정교화.
 
 ---
 
 ## 📅 2026-05-11 | v24.5 [The Unified Soul: Data Consistency Hardening]
 
 ### 1. [Context] 오늘의 이슈
 *   서버 파이프라인 구동 시, 텔레그램 리포트의 '주제(삼성 AI 글래스)'와 '내용(현대차 로봇)'이 서로 다르게 섞여 발송되는 '키메라 현상' 발견.
 *   원인: 에이전트 간의 독립적 사냥 결과 불일치 및 퍼블리셔의 파편화된 데이터 로딩 방식.
 
 ### 2. [Milestones] 성공한 것
 *   **사냥 결과 단일화(Single Source of Truth)**:
     *   `WriterAgent`가 앞단(Detector)에서 결정된 사냥 결과를 우선적으로 따르도록 수정하여 서사의 일관성 확보.
 *   **패키지 단위 로딩 시스템 도입**:
     *   `PublisherAgent`가 특정 `Topic_N` 폴더를 선정하면 그 안의 모든 데이터(시그널, 대본, 분석)를 한 세트로 가져오도록 로직 전면 개편.
     *   루트 경로의 `today_signal.json`보다 개별 토픽 폴더의 데이터를 우선시하여 주제-내용 간의 100% 매칭 보장.
 
 ### 3. [Pitfalls] 오늘의 삽질 및 교훈
 *   **유연한 경로의 함정**: 파일별로 가장 최신 버전을 찾도록 설계된 로직이, 병렬 실행이나 부분 실패 시 서로 다른 회차의 데이터를 조립할 위험이 있었음. -> **교훈**: 데이터는 파일 단위가 아닌 '세션/패키지 단위'로 취급해야 함.
 
 ### 4. [Next Step] 이어서 해야 할 일
 1.  **DART API 패키지 안정화**: `OpenDartReader` 등 금융 데이터 수집기 복구.
 2.  **이미지 생성 프롬프트 정교화**: 토픽의 성격에 맞춰 Imagen 4.0이 더 극적인 커버 이미지를 만들 수 있도록 가이드 보강.

---

## 📅 2026-05-11 (v24.5 [The Hardened Two-Track Hunter] 시스템 요새화)

### 1. [Context] 오늘의 시작
* 경제사냥꾼의 논리 체계를 완벽히 이식하기 위한 '투트랙 사냥' 전략 수립 및 시스템 전반의 요새화(Hardening) 작업 수행.
* 유튜브 대본 수집 실패 및 데이터 동기화 충돌 이슈 해결 요청.

### 2. [Milestones] 성공한 것
* **투트랙 사냥 전략(Two-Track Hunting) 도입**: 
    * 글로벌 매크로(Track A)와 국내 구조적 특수성(Track B)을 동시에 분석하는 Arbiter v21.0 런칭.
* **유튜브 데이터 영토 격리 (Isolated Learning)**: 
    * `data/`와 혼용되던 유튜브 데이터를 `youtube_data/` 최상위 폴더로 완전 분리. 충돌 원천 차단.
* **자막 추출 엔진 수리**: 
    * `yt-dlp` 기반의 튼튼한 추출 엔진으로 교체하여 사냥꾼의 대본 7건(로봇주 상장 등) 완벽 확보.
* **대시보드 기능 통합**: 
    * 카드뉴스 뷰어(`instaviewer.html`)에 '사냥 보고서 전문 읽기' 모달 탑재 및 경로 최적화.
* **자동화 스케줄 최적화**: 
    * 매일 4회(02, 08, 14, 20 KST) 정기 사냥 및 1시간 전 유튜브 학습 자동화 셋팅 완료.

### 3. [Pitfalls] 오늘의 삽질 및 교훈
* **Git Rebase Conflict**: 로컬과 서버의 데이터 파일이 엉키며 액션이 멈춤. -> **해결**: `git pull --rebase -X theirs` 옵션을 통해 서버 데이터 우선 합병 전략 도입.
* **Path Confusion**: 언더바 유무(`insta_viewer` vs `instaviewer`)로 인한 링크 오류. -> **해결**: 두 파일 모두 지원하도록 미러링 파일 생성.

### 4. [Next Step] 이어서 해야 할 일
1. **02시 첫 자동 사냥 모니터링**: 스케줄러가 정상 작동하고 텔레그램 알림이 잘 오는지 확인.
---

## 📅 2026-05-12 | v26.6 [The Resilient Hunter: Cost-Optimization & Crash Mastery]

### 1. [Context] 오늘의 시작
*   기존 Pro 모델 기반 파이프라인의 비용 폭증(12일 만에 예산 소진 위기) 및 서버 모델 404 에러로 인한 시스템 마비 상태에서 작업 시작.
*   KOSPI 8,000선 붕괴라는 시장 패닉 상황에 대한 사냥꾼의 독자적 시각 부재 문제 해결 요청.

### 2. [Milestones] 성공한 것
*   **초저비용 가성비 모드 (v26.6) 안착**:
    *   **모델 표준화**: 2026년형 최신 모델인 `gemini-flash-latest`로 Arbiter 및 DeepResearch 전면 교체 (비용 95% 절감).
    *   **유튜브 수집 비용 제로화**: `LearnerAgent`의 AI 분석 단계를 일시 중단(`PAUSED`)하고 파일 보관 위주로 개편하여 수집 비용 0원 달성.
*   **시장 하락장 분석 로직(Market Crash Priority) 주입**:
    *   패닉 셀링 속에서 '물리적 병목'과 '스마트머니 피난처'를 찾는 특수 지침을 Arbiter에 주입.
    *   **결과**: 오늘 자 리포트에서 **ESS(LS ELECTRIC)**와 **액침냉각(GST)**을 하락장의 역설적 수혜주로 포착 성공.
*   **인프라 안정성 강화**:
    *   서버 워크플로우와 로컬 간의 로그 파일 Git 충돌 문제를 강제 동기화 전략으로 해결.

### 3. [Pitfalls] 오늘의 삽질 및 교훈
*   **Model Naming 404**: 2026년 환경에서는 `gemini-1.5-flash` 등의 구형 명칭이 작동하지 않음을 확인. -> **해결**: SDK를 통해 실제 모델 리스트를 조회하여 `gemini-flash-latest`가 최적임을 판별.
*   **Git Conflict Loop**: 서버 가동 중인 로그 파일을 건드려 충돌이 무한 반복됨. -> **교훈**: 로그와 데이터는 서버를 우선시하고 코드는 로컬을 우선시하는 명확한 체크아웃 전략 필수.

### 4. [Next Step] 이어서 해야 할 일
1.  **반등 섹터 선점**: 오늘 포착된 ESS, 액침냉각 섹터의 실질 수급 확인 및 차기 사냥 토픽 연결.
2.  **배치 학습 프로세스 설계**: 보관된 대본들을 한꺼번에 학습시키는 저비용 DNA 패치 자동화 로직 구현.

---

## 📅 2026-05-12 | v25.0 [The Adaptive Hunter: Autonomous DNA Patching]

### 1. [Milestones] 성공한 것
* **DNA Patch 자동화 시스템 (v25.0) 런칭**:
    * **DNAManager**: `youtube_data/history/dna_evolution.json`에서 사냥꾼의 최신 학습 데이터를 실시간으로 읽어와 프롬프트에 주입하는 자율 진화 체계 구축.
    * **전략 사냥꾼(Arbiter) & 수석 전략가(Writer) 연동**: 토픽 선정과 원고 작성 시 최신 DNA 패치가 자동으로 적용되어, 별도의 코드 수정 없이도 엔진의 논리와 문체가 지속적으로 업그레이드됨.
    * **패치 로깅 시스템**: `dna_patch_log.json`을 통해 각 에이전트별 DNA 적용 이력을 투명하게 추적.
* **DART 수집기 요새화 (Stabilization)**:
    * **Retry & Backoff**: DART 상세 문서 추출 시 발생하는 014(파일 없음) 오류 및 일시적 통신 장애에 대응하는 2단계 재시도 로직 도입.
    * **XML 파서 전환**: BeautifulSoup 사용 시 발생하던 `XMLParsedAsHTMLWarning`을 `features="xml"` 적용으로 완벽 제거.
* **로컬 파이프라인 무결성 확인**:
    * `run_full_pipeline.py`를 통해 Collector부터 Publisher까지의 전 과정이 로컬 환경에서 정상 작동함을 재검증.

### 2. [Pitfalls] 오늘의 삽질 및 교훈
* **Parser Mismatch**: DART 응답이 HTML 형식을 띤 XML인 경우가 많아 일반 HTML 파서로는 경고가 발생함. -> **해결**: `lxml` 기반의 `xml` 파서를 명시적으로 지정하여 데이터 파싱의 정확도와 성능을 동시에 잡음.
* **DNA Freshness**: 단순히 모든 패치를 넣으면 프롬프트가 너무 길어짐. -> **해결**: `limit=3` 정책을 도입하여 가장 최신의, 가장 날카로운 통찰만 우선적으로 주입하도록 최적화.

### 3. [Next Step] 이어서 해야 할 일
1. **DNA 패치 실효성 모니터링**: 자동 주입된 DNA가 실제 원고의 '격'을 얼마나 높이는지 정성적 평가 수행.
2. **카드뉴스 템플릿 가변화**: 토픽의 성격(상승/하락/이상징후)에 따라 카드뉴스 배경색과 레이아웃을 동적으로 변경하는 기능 추가.
3. **서버 배포 및 동기화**: 로컬에서 개선된 DNA 자율 패칭 시스템을 GitHub Actions 환경에 배포.

---

## 📅 2026-05-14 | v5.5 [The Waterflow Intelligence: Market Rotation Radar]

### 1. [Context] 오늘의 시작
*   경제사냥꾼의 "순환매 시나리오"를 주관적 판단이 아닌 실시간 데이터(DART + Pykrx)로 검증하고 시각화하려는 시도.
*   "돈의 물줄기가 어디에 머물러 있는가?"를 한눈에 파악하기 위한 전용 대시보드 구축 요청.

### 2. [Milestones] 성공한 것
*   **순환매 인텔리전스 엔진 (RotationRadar) 구축**:
    *   **15일 물줄기 히트맵 (Waterflow Timeline)**: 최근 15거래일간의 섹터별 에너지 강도를 히트맵으로 시각화하여 자본 이동 궤적 추적 성공.
    *   **에너지 체온계 (Energy Heat Gauge)**: 각 단계별 '열기'를 체온계 스타일로 시각화하고, 15일 내 최고점(Peak Marker) 표시 기능 탑재.
    *   **모멘텀 지표 (Momentum Indicator)**: 어제와 오늘을 비교하여 `👑(Peak)`, `↑(Rising)`, `↓(Falling)` 아이콘으로 에너지의 방향성 판별.
    *   **자동 FOCUS 탐지**: 오늘 수익률이 가장 높은 단계를 시스템이 자동으로 판단하여 `[CURRENT]` 배지 및 상단 헤드라인 마킹.
*   **메인 파이프라인 완전 통합 (Autonomous Integration)**:
    *   `run_full_pipeline.py`의 핵심 공정으로 `ROTATION` 단계를 공식 추가.
    *   서버 자동 스케줄러 가동 시 뉴스 사냥과 동시에 순환매 분석 및 대시보드 갱신이 원스톱으로 이루어지도록 통합 완료.
*   **동적 지능 파이프라인 (Dynamic Architecture)**:
    *   `market_context.json`을 도입하여 소스 코드 수정 없이 설정 파일만으로 단계별 섹터/종목/키워드 관리가 가능하도록 개편.
    *   `financial_collectors.py` 내 잔존하던 하드코딩 업종 리스트 완전 제거 및 정예화.
*   **충돌 해결 및 배포**:
    *   서버 파이프라인 데이터와 로컬 코드 간의 대규모 충돌(Conflict)을 '백업 후 강제 정렬' 전략으로 해결하고 서버(origin/main) 푸시 완료.

### 3. [Pitfalls] 오늘의 삽질 및 교훈
*   **Git Divergence**: 서버에서 생성되는 자동 업데이트 데이터와 로컬의 소스 수정이 엉켜 `git push`가 거절됨. -> **해결**: 핵심 코드를 `/tmp`에 백업 후 `git reset --hard origin/main`으로 정렬하고 다시 주입하는 방식으로 가장 안전하게 해결.
*   **UI/UX 정교화**: 단순히 수치만 보여주는 것보다 '온도계'나 '물줄기' 같은 비유적 시각화가 사용자 이해도에 훨씬 큰 영향을 미침을 확인.

### 4. [Next Step] 이어서 해야 할 일
1.  **텔레그램 푸시 연동**: 특정 섹터가 `👑(Peak)`를 찍거나 에너지가 5.0% 이상 돌파 시 즉시 알림 발송.
2.  **데이터 소스 확장**: 현재의 DART 수주 공시 외에 '투자자별 순매수 데이터'의 비중을 높여 수급 분석 정교화.
3.  **히트맵 상세 모달**: 히트맵의 각 칸을 클릭하면 당시 어떤 공시나 뉴스가 에너지를 폭발시켰는지 팝업으로 보여주는 기능.
