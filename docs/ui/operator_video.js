/**
 * operator_video.js — VIDEO CANDIDATE POOL & SCRIPT ADAPTER
 * Premium Tailwind-based Rendering for Phase 22A
 */

export async function initVideoView(container) {
    container.innerHTML = `
        <div class="p-8 flex flex-col items-center justify-center space-y-4">
            <div class="w-8 h-8 border-2 border-blue-500 border-t-transparent rounded-full animate-spin"></div>
            <div class="text-[10px] font-black text-slate-500 uppercase tracking-[0.2em]">Loading Video Intelligence...</div>
        </div>
    `;

    try {
        const poolPaths = ['./data/ops/video_candidate_pool.json', '../data/ops/video_candidate_pool.json', './docs/data/ops/video_candidate_pool.json'];
        const scriptPaths = ['./data/ops/video_script_pack.json', '../data/ops/video_script_pack.json', './docs/data/ops/video_script_pack.json'];

        let poolData = null;
        for (const path of poolPaths) {
            try {
                const res = await fetch(path);
                if (res.ok) { poolData = await res.json(); break; }
            } catch (e) { }
        }

        let scriptData = null;
        for (const path of scriptPaths) {
            try {
                const res = await fetch(path);
                if (res.ok) { scriptData = await res.json(); break; }
            } catch (e) { }
        }

        if (!poolData || !poolData.top_candidates || poolData.top_candidates.length === 0) {
            renderEmptyVideoState(container);
            return;
        }

        const mergedCandidates = poolData.top_candidates.map(c => {
            const sMatch = scriptData?.candidates?.find(sc => sc.dataset_id === c.dataset_id);
            return sMatch ? { ...c, script: sMatch.script } : c;
        });

        renderVideoCandidates(container, mergedCandidates);
    } catch (err) {
        console.error('[VideoView] Error:', err);
        container.innerHTML = `<div class="p-8 text-red-500 font-mono text-xs bg-red-500/5 rounded border border-red-500/20 m-4">[VIDEO_ERROR] Load Failed: ${err.message}</div>`;
    }
}

function renderEmptyVideoState(container) {
    container.innerHTML = `
        <div class="max-w-4xl mx-auto py-20 px-4 text-center">
            <div class="text-6xl mb-6 opacity-20">🎬</div>
            <h3 class="text-xl font-black text-slate-400 mb-2 uppercase tracking-tighter">현재 선별된 영상 후보가 없습니다</h3>
            <p class="text-sm text-slate-500 max-w-md mx-auto leading-relaxed">NS 점수 및 구조적 조건(Axis, Trigger)을 충족하는 지능형 선별 토픽이 발견되면 이곳에 자동으로 표시됩니다.</p>
        </div>
    `;
}

function renderVideoCandidates(container, candidates) {
    candidates.sort((a, b) => (b.video_score || 0) - (a.video_score || 0));
    const top3 = candidates.slice(0, 3);

    container.innerHTML = `
        <div class="space-y-8 fade-in pb-20">
            <header class="mb-10 px-4">
                <div class="text-[10px] font-black text-blue-500 uppercase tracking-[0.4em] mb-2">Video Intelligence Layer v1.0</div>
                <h1 class="text-4xl font-black text-white tracking-tighter uppercase leading-none">편집 회의 보드</h1>
                <p class="text-slate-500 text-sm mt-4 font-medium max-w-2xl">상위 3개 토픽과 자동 생성된 **영상 스크립트 패키지**입니다.</p>
            </header>
            <div class="grid gap-8 px-4">${top3.map((c, idx) => renderVideoCard(c, idx)).join('')}</div>
            <footer class="mt-20 py-10 border-t border-slate-800 text-center opacity-30 px-4"><div class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Generated by HOIN Script Intelligence Engine v1.0</div></footer>
        </div>
    `;
}

function renderVideoCard(c, idx) {
    const isTop = idx === 0;
    const borderClass = isTop ? 'border-amber-500/50' : 'border-slate-800';
    const bgClass = isTop ? 'bg-amber-500/5' : 'bg-slate-900/50';
    const ns = c.narrative_score ?? 'N/A';
    const vs = c.video_score ?? 'N/A';
    const intensity = c.intensity != null ? `${c.intensity}%` : 'N/A';
    const conflict = c.conflict_flag ? '<span class="text-red-500 animate-pulse">⚠️ CONFLICT</span>' : '';

    const script = c.script;
    const scriptSection = script ? `
        <div class="mt-8 pt-8 border-t border-slate-800/50 space-y-6">
            <div class="flex items-center gap-2 mb-2"><span class="w-1.5 h-1.5 rounded-full bg-blue-500 animate-ping"></span><span class="text-[10px] font-black text-blue-500 uppercase tracking-widest">Auto-Generated Script</span></div>
            <div class="space-y-4">
                <div class="bg-blue-500/5 border border-blue-500/20 rounded-xl p-4"><div class="text-[9px] font-black text-blue-400 uppercase tracking-widest mb-2 opacity-60">HOOK</div><div class="text-sm font-bold text-white leading-relaxed">"${script.hook}"</div></div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div class="bg-black/40 border border-slate-800 rounded-xl p-4"><div class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-3">1분 요약 (3-Lines)</div><ul class="text-[11px] text-slate-300 space-y-2 leading-relaxed">${script.one_min_summary_3lines.map(l => `<li class="flex gap-2"><span class="text-blue-500">•</span> ${l}</li>`).join('')}</ul></div>
                    <div class="bg-black/40 border border-slate-800 rounded-xl p-4"><div class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-3">인과 사슬 (Causal Chain)</div><div class="space-y-2 text-[11px]"><div class="flex justify-between items-center"><span class="text-slate-500 uppercase font-bold">CAUSE</span> <span class="text-white font-medium">${script.causal_chain.cause || 'N/A'}</span></div><div class="flex justify-between items-center"><span class="text-slate-500 uppercase font-bold">SHIFT</span> <span class="text-white font-medium">${script.causal_chain.structural_shift || 'N/A'}</span></div><div class="flex justify-between items-center"><span class="text-slate-500 uppercase font-bold">RESULT</span> <span class="text-white font-medium">${script.causal_chain.market_consequence || 'N/A'}</span></div></div></div>
                </div>
                <div class="bg-black/20 border border-slate-800/50 rounded-xl p-4"><div class="text-[9px] font-black text-slate-500 uppercase tracking-widest mb-2">언급 가능 종목 및 리스크</div><div class="flex flex-wrap gap-2">${script.mentionables.map(m => `<div class="bg-slate-800 px-3 py-2 rounded-lg border border-slate-700"><div class="text-[11px] font-black text-white">${m.ticker}</div><div class="text-[9px] text-slate-500">${m.reason}</div></div>`).join('')}</div></div>
            </div>
        </div>
    ` : `<div class="mt-8 pt-8 border-t border-slate-800/50 text-center py-4"><span class="text-[10px] font-black text-slate-600 uppercase tracking-widest">Script Generation in progress...</span></div>`;

    return `<div class="${bgClass} border ${borderClass} rounded-2xl p-8 relative overflow-hidden group transition-all duration-300 hover:border-blue-500/30">${isTop ? '<div class="absolute top-0 left-0 w-1.5 h-full bg-amber-500"></div>' : ''}<div class="flex flex-wrap justify-between items-center mb-6 gap-4"><div class="flex items-center gap-3"><span class="bg-slate-800 text-white text-[10px] font-black px-3 py-1 rounded-full border border-slate-700 tracking-widest">RANK ${idx + 1}</span><span class="text-[10px] font-black text-slate-500 uppercase">${c.dataset_id || 'unknown'}</span></div><div class="font-black text-[10px] tracking-widest uppercase">${conflict}</div></div><h2 class="text-2xl font-black text-white mb-6 group-hover:text-amber-500 transition-colors tracking-tight leading-tight">${c.title || 'Untitled Topic'}</h2><div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4"><div class="space-y-1"><div class="text-slate-500 font-bold uppercase tracking-wider text-[9px]">VIDEO SCORE</div><div class="text-amber-500 font-black text-lg">${vs}</div></div><div class="space-y-1"><div class="text-slate-500 font-bold uppercase tracking-wider text-[9px]">NARRATIVE</div><div class="text-blue-500 font-black text-lg">${ns}</div></div><div class="space-y-1 text-center md:text-left"><div class="text-slate-500 font-bold uppercase tracking-wider text-[9px]">INTENSITY</div><div class="text-white font-black text-lg">${intensity}</div></div><div class="space-y-1 text-right"><div class="text-slate-500 font-bold uppercase tracking-wider text-[9px]">TRIGGER</div><div class="text-white font-black text-lg">${c.why_now_type || 'N/A'}</div></div></div>${scriptSection}</div>`;
}
